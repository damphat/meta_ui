using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Provider : MonoBehaviour
{
    void On<T>(string name, Action<T> action)
    {
        
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
